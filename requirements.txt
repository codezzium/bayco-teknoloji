Django>=5.1,<5.2

# Ortak Postgres (shared-pgbouncer) sürücüsü. SQLite fallback duruyor; bu paket
# yalnızca DB_HOST tanımlıyken devreye girer.
psycopg[binary]>=3.2,<4

# Üst sınır ŞART: yeni sunucuda imaj sıfırdan derleniyor, sınırsız bırakılırsa
# o gün çıkmış büyük sürüm gelir. Canlıda Pillow 12.3.0 ve whitenoise 6.12.0 var.
Pillow>=12,<13
whitenoise>=6.12,<7

# Stok modülü — etiket üretimi (ikisi de saf Python, transitive bağımlılık yok)
segno>=1.6,<2
python-barcode>=0.16,<0.17
# TruncMonth + USE_TZ: sistem tz veritabanı olmayan slim imajlarda sessizce
# UTC'ye düşer ve akşam satışları yanlış aya yazılır.
tzdata
